name: build my node application
on: [push]

jobs:
    build_node_application:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@4
            - uses: actions/setup-node@4
              with:
                node-version: '20'
            - run: npm install 
            - run: node server.js